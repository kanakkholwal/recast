/** Colour/preview swatch tiles: an offset ring so a card-coloured gap separates it from any swatch. */
export const SWATCH_SELECTED = "ring-2 ring-foreground/60 ring-offset-1 ring-offset-card";

/** Surface tiles and list rows (layers, custom-background, templates): the app's house selected token. */
export const CARD_SELECTED = "border-foreground/40 bg-card ring-1 ring-inset ring-foreground/20";

/** Idle for surface tiles/rows, paired with CARD_SELECTED. */
export const CARD_IDLE = "border-border hover:bg-accent";
