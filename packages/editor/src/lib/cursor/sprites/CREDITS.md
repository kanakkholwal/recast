# Cursor sprite credits

Most sprites in this folder (`dot`, `macos-*`, `windows-*`, `outline-*`, `target*`)
are original Recast artwork.

The **`system-macos-*`** and **`system-windows-*`** sprites reproduce the
**Apple macOS** and **Microsoft Windows** system cursors. Each file was wrapped
into Recast's 64×64 sprite canvas (a `translate`+`scale` group); the cursor
shapes themselves are the respective vendors' designs.

> ⚠️ **Provenance / licensing: review before shipping publicly.**
> These depict the **Apple macOS** (`system-macos-*`) and **Microsoft Windows**
> (`system-windows-*`) system cursors. Those cursor *designs* are Apple /
> Microsoft intellectual property. Imported at the product owner's explicit
> direction; confirm redistribution terms with the respective vendors before a
> public release.

| Recast sprite | Cursor |
| --- | --- |
| `system-macos-arrow.svg` | macOS arrow |
| `system-macos-pointer.svg` | macOS link pointer (hand) |
| `system-macos-context.svg` | macOS contextual menu |
| `system-macos-grab.svg` | macOS closed hand (grab) |
| `system-windows-arrow.svg` | Windows arrow |
| `system-windows-hand.svg` | Windows link hand |
| `system-windows-move.svg` | Windows move (size-all) |
