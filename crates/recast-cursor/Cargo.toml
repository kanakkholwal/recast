[package]
name = "recast-cursor"
version = "0.0.0"
edition = "2021"
publish = false
description = "Cursor track model, path smoothing and click animation curves. One implementation for preview and export."

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.151"
serde = { version = "1.0", features = ["derive"] }
